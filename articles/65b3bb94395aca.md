---
title: "Async-graphqlでDataloaderを使用してN+1問題の解決"
emoji: "🦸"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["rust", "graphql", "asyncgraphql", "dataloader", "seaorm"]
published: false
---

# 概要

Rust の Async-graphql で GraphQL サーバの実装を試しています。
1:N 関係の構造で何も考えずに実装すると N+1 問題が発生するため、Dataloader を使って解決してみました。

# サンプル

今回作成したサンプルは下記です。

https://github.com/ne0san/async-graphql-dataloader-sample

# 今回の前提条件

以下のクレートを使用する例です。

- Async-graphql
- Actix-web
- SeaORM

:::details 具体的な依存関係

```toml:Cargo.toml
[dependencies]
dataloader = { version = "0.17.0", features = ["tokio"] }
dotenv = "0.15.0"
sea-orm = { version = "0.12.15", features = [
  "sqlx-mysql",
  "runtime-tokio-native-tls",
  "macros",
  "debug-print"
] }
sea-orm-migration = "0.12.15"
entity = { path = "./entity" }
futures = "0.3.30"
chrono = "0.4.38"
chrono-tz = "0.9.0"
tracing = "0.1.40"
tracing-subscriber = "0.3.18"
actix-web = "4.8.0"
async-graphql = { version = "7.0.7", features = ["dataloader"] }
async-graphql-actix-web = "7.0.7"
lambda-web = { version = "0.2.1", features = ["actix4"] }
tokio = "1.39.2"
```

:::

## スキーマ

```graphql
type Item {
  id: ID!
  name: String!
  description: String
  user: User
}

type Query {
  user(id: ID): [User!]!
  item(id: ID): [Item!]!
}

type User {
  id: ID!
  name: String!
  description: String
  items: [Item!]
}
```

## DB 構造

1:n 関係をもつ user および item テーブルで試しました。

```sql
CREATE TABLE `user` (
  `id` int NOT NULL AUTO_INCREMENT,
  `name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `description` text COLLATE utf8mb4_unicode_ci,
  `deleted` tinyint(1) NOT NULL DEFAULT '0',
  `created_at` timestamp NOT NULL,
  `updated_at` timestamp NOT NULL,
  PRIMARY KEY (`id`)
);

CREATE TABLE `item` (
  `id` int NOT NULL AUTO_INCREMENT,
  `user_id` int NOT NULL,
  `name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL,
  `description` text COLLATE utf8mb4_unicode_ci,
  `deleted` tinyint(1) NOT NULL DEFAULT '0',
  `created_at` timestamp NOT NULL,
  `updated_at` timestamp NOT NULL,
  PRIMARY KEY (`id`),
  KEY `FK_item_user` (`user_id`),
  CONSTRAINT `FK_item_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`)
);
```

SeaORM での Entity 定義は下記の通りです。

```rust:user.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "user")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub id: i32,
    pub name: String,
    #[sea_orm(column_type = "Text", nullable)]
    pub description: Option<String>,
    pub deleted: bool,
    pub created_at: DateTimeWithTimeZone,
    pub updated_at: DateTimeWithTimeZone,
}

#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {
    Item,
}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        match self {
            Self::Item => Entity::belongs_to(super::item::Entity)
                .from(Column::Id)
                .to(super::item::Column::UserId)
                .into(),
        }
    }
}

impl Related<super::item::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::Item.def()
    }
}

impl ActiveModelBehavior for ActiveModel {}
```

```rust:item.rs
use sea_orm::entity::prelude::*;

#[derive(Clone, Debug, PartialEq, DeriveEntityModel)]
#[sea_orm(table_name = "item")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub id: i32,
    pub name: String,
    pub user_id: i32,
    #[sea_orm(column_type = "Text", nullable)]
    pub description: Option<String>,
    pub deleted: bool,
    pub created_at: DateTimeWithTimeZone,
    pub updated_at: DateTimeWithTimeZone,
}

#[derive(Copy, Clone, Debug, EnumIter)]
pub enum Relation {
    User,
}

impl Related<super::user::Entity> for Entity {
    fn to() -> RelationDef {
        Relation::User.def()
    }
}

impl RelationTrait for Relation {
    fn def(&self) -> RelationDef {
        match self {
            Self::User => Entity::has_many(super::user::Entity)
                .from(Column::UserId)
                .to(super::user::Column::Id)
                .into(),
        }
    }
}

impl ActiveModelBehavior for ActiveModel {}
```

## 下準備

### プロジェクト作成 & インストール

### テーブル作成 & 初期データ投入

# N+1 問題が発生する例

# Dataloader を使う例
