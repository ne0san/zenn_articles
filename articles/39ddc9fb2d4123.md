---
title: "RustでAtCoderに挑む際の環境構築・設定まとめ"
emoji: "⚙️"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["atcoder", "rust", "vscode"]
published: false
---

# 概要

Rust で AtCoder に挑む際の環境構築の手順と、私個人の設定を色々まとめました。

# 環境構築

:::message
以下それぞれインストール済みの状態を前提としています。

- VSCode
- Homebrew (Mac の場合)

:::

## Rustup のインストール

`rustup` と併せて rust のパッケージ管理ツールである `cargo` および、
rust コンパイラの `rustc` もインストールされます。

### Windows

```powershell
Invoke-WebRequest -Uri 'https://aka.ms/vs/17/release/vs_BuildTools.exe' -OutFile "$env:TEMP\vs_BuildTools.exe" & "$env:TEMP\vs_BuildTools.exe" --passive --wait --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended --remove Microsoft.VisualStudio.Component.VC.CMake.Project	# Visual C++をインストール
winget install rustup # rustupをインストール
```

### Mac

```zsh
brew install rustup-init # rustup-initをインストール
rustup-init # rustup-initを実行
```

## cargo-compete のインストール

```zsh
cargo install cargo-compete
```

## ジャッジシステムに合わせた Rust のインストール

ジャッジシステム更新によってバージョンが変動するため、

適宜 AtCoder 公式の言語一覧をご参照ください。

https://img.atcoder.jp/file/language-update/language-list.html

```zsh
rustup install 1.70.0
```

# 設定

# 回答時の手順

# Tips

# 参考記事
