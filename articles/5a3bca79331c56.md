---
title: "terraformのbackendについて深堀"
emoji: "📝"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["terraform"]
published: false
---

# 概要

同じ Terraform のソースから複数の環境を作成するため、

`*.tf`ファイルにバックエンドの設定を記載せずに`.tfbackend`ファイルを使用して定義しようとしたところ少しハマったので

terraform の backend に関して深堀して調べて見ることにしました。

https://developer.hashicorp.com/terraform/language/settings/backends/configuration

:::details 試したこと
個人開発で以下のように試したところ、backend が local 扱いになりました。

https://zenn.dev/link/comments/81a6eb77ddd22a

main.tf には特に backend の定義はなし

```tf:main.tf
# ------------------------
# - Terraform configuratoions
# ------------------------
terraform {
  required_version = ">1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~>3.0"
    }
  }
}
```

dev 環境用の tfbackend ファイルを作成

```tf:backend/dev.tfbackend
bucket = ****
key    = "****dev.tfstate"
region = "ap-northeast-1"
profile = ****
```

作成した tfbackend を使用して terraform init

```shell
terraform init -reconfigure -backend-config="backend/dev.tfbackend"
```

warn が出力される。
backend の定義が見つからず、local 扱いになっていた。

```
Initializing the backend...

Initializing provider plugins...
- Reusing previous version of hashicorp/aws from the dependency lock file
- Using previously-installed hashicorp/aws v3.76.1

╷
│ Warning: Missing backend configuration
│
│ -backend-config was used without a "backend" block in the configuration.
│
│ If you intended to override the default local backend configuration,
│ no action is required, but you may add an explicit backend block to your
│ configuration to clear this warning:
│
│ terraform {
│   backend "local" {}
│ }
│
│ However, if you intended to override a defined backend, please verify that
│ the backend configuration is present and valid.
│
╵
```

main.tf に backend の設定を記載すると解決しました。

```tf:main.tf
# ------------------------
# - Terraform configuratoions
# ------------------------
terraform {
  required_version = ">1.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~>3.0"
    }
  }
  backend "s3" {
    bucket = ****
    key    = "****dev.tfstate"
    region = "ap-northeast-1"
    profile = ****
  }
}
```

backend の設定は別ファイルに分離したかった....

:::

# バックエンドの設定手順

トップレベルの terraform ブロックにネストしたバックエンドブロックを追加

# `.tfbackend`ファイルとは

# `.tfbackend`ファイルの使い方

tfbackend がうまく反映できてなかった。

なんで詰んだのか

tfbackend について深堀する。
